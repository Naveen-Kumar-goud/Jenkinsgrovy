pipeline{
        agent any
        options{
                timestamps()
                retry(3)
        }
        parameters{
                choice(name: 'CHOICE', choices: 'YES\nNO', description: 'SELECT ONE')
        }
        stages{
                stage('DRYRUN'){
                        steps{
                                script{
                                        if("$CHOICE" == "YES")
                                        {
                                                currentBuild.result = "ABORTED"
                                                error("DRY RUN")
                                        }
                                }
                        }
                }
        }
}
