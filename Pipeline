pipeline{
        agent any
        parameters{
                choice(name: 'CHOICE', choices: 'YES\nNO', description: 'SELECT ONE')
        }
        stages{
                stage('DRYRUN'){
                        steps{
                                echo "$CHOICE"
                        }
                }
        }
}
