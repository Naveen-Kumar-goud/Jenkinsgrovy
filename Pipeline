pipeline{
        agent any
        parameters{
                choice(name: 'CHOICE', choices: 'YES\nNO', description: 'SELECT ONE')
        }
        stages{
                stage('DRYRUN'){
                        steps{
                                script{
                                        if("$CHOICE" == "YES")
                                        {
                                                currentBuild.result = "ABORTED"
                                                error("DRY RUN")
                                        }
                                }
                        }
                }
        }
}
